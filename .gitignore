node_modules/
dist/
build/
.env
*.log
.DS_Store
agent-service/reports/
agent-service/.playwright-mcp/
# Test-suite snapshots — repo root, not nested under agent-service/, since
# a snapshot bundles content from both agent-service/ and tests/.
/archive/
